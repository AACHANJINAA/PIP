# 상용 엔진(Unity/Unreal) 대비 고도화 및 폴리싱 상세 계획서

본 문서는 유니티와 언리얼 엔진에서 제공하는 핵심 기능들을 분석하여, 자체 제작 엔진 기반의 **"Slay The Lord"** 프로젝트가 액션 RPG로서의 완성도를 갖추기 위해 도입해야 할 필수 시스템들을 제안합니다.

---

## 1. 클라이언트 고도화: "타격감과 시각적 완성도"

상용 엔진의 '손맛'과 '연출'을 자체 엔진에서 구현하기 위한 핵심 시스템입니다.

### 1.1. 애니메이션 이벤트 시스템 (AnimNotify / Animation Event)
*   **상용 엔진 방식:** 애니메이션의 특정 프레임에 소리 재생, 이펙트 생성, 공격 판정 시작 등의 이벤트를 시각적으로 배치.
*   **도입 제안:** `AnimationComponent` 확장.
    *   **구현:** JSON 데이터에 애니메이션별 이벤트 정의. 
    *   *예시:* `{"anim": "Attack_01", "triggers": [{"frame": 10, "type": "SOUND", "id": "Sword_Swing"}, {"frame": 15, "type": "EFFECT", "id": "Trail"}]}`
*   **효과:** 공격 동작과 시각/청각 효과의 완벽한 일치 (타격감의 기초).

### 1.2. 카메라 연출 시스템 (Camera Shake & Interpolation)
*   **상용 엔진 방식:** Cinemachine(Unity)이나 Camera Shake Class(UE)를 통해 타격 시 화면 흔들림, 피사체 추적 연출.
*   **도입 제안:** `CameraController` 기능 강화.
    *   **구현:** `Shake(intensity, duration)`, `Zoom(fov, speed)` 함수 추가. Perlin Noise를 활용한 자연스러운 흔들림 구현.
*   **효과:** 강력한 공격을 맞히거나 맞았을 때의 '무게감' 전달.

### 1.3. 히트 스톱 및 역경직 (Hit Stop / Time Dilation)
*   **상용 엔진 방식:** `Time.timeScale`이나 `GlobalTimeDilation`을 조절하여 타격 순간 게임 속도를 일시 정지.
*   **도입 제안:** `TimerManager` 전역 배율 기능.
    *   **구현:** 타격 발생 시 `TimerManager::SetTimeScale(0.05f)` 호출 후 0.1초 뒤 원복.
*   **효과:** 칼이 뼈에 박히는 듯한 '쫀득한' 손맛 구현.

### 1.4. 데칼 렌더링 시스템 (Decal / Projector)
*   **상용 엔진 방식:** 바닥이나 벽에 텍스처를 투영. 장판 공격 범위 표시에 필수적임.
*   **도입 제안:** **Decal Shader** 추가.
    *   **구현:** 큐브(Box) 영역 안에 들어오는 지형 픽셀에 특정 텍스처를 투영해서 그림.
*   **효과:** 보스의 광역 공격 범위를 바닥에 빨간색/노란색으로 표시하여 전략적 플레이 유도.

---

## 2. 서버 고도화: "개발 효율과 운영 안정성"

서버 프로그래머가 로직에만 집중하고, 기획자가 실시간으로 밸런스를 잡기 위한 시스템입니다.

### 2.1. 데이터 핫 리로드 (Hot-Reload / Live Config)
*   **상용 엔진 방식:** 서버를 끄지 않고도 에디터에서 수치를 바꾸면 즉시 반영.
*   **도입 제안:** **File Watcher** 기반 실시간 로더.
    *   **구현:** `std::filesystem`을 사용하여 JSON 파일의 수정 시간을 감시. 변경 시 `DataManager`가 해당 파일만 다시 로드.
*   **효과:** 보스 난이도 조절 시 서버 재시작 시간(약 10~30초)을 0초로 단축.

### 2.2. 콘솔 치트 및 명령어 시스템 (Cheat Manager)
*   **상용 엔진 방식:** `~` 키를 눌러 명령어를 입력하여 디버깅 (예: `slomo 0.5`, `spawn monster`).
*   **도입 제안:** **Chat Command Parser**.
    *   **구현:** 채팅 패킷 중 `/`로 시작하는 메시지를 서버가 별도로 파싱하여 내부 함수 실행.
*   **효과:** 특정 스테이지 바로 가기, 무적 모드 등 QA 및 개발 테스트 효율 극대화.

### 2.3. 비동기 로깅 및 미니덤프 (Logging & Crash Report)
*   **상용 엔진 방식:** 서버 크래시 시 로그와 덤프 파일을 자동 생성하여 원인 분석 지원.
*   **도입 제안:** **Async Logger & MiniDump**.
    *   **구현:** 로깅 전용 스레드를 운영하여 디스크 I/O 병목 제거. `SetUnhandledExceptionFilter`를 통해 크래시 순간의 상태 저장.
*   **효과:** "서버가 왜 죽었는지 모르는" 상황 방지.

---

## 3. 요약 및 비교 테이블

| 구분 | 상용 엔진 기능 | 현재 프로젝트 도입 목표 | 우선순위 |
| :--- | :--- | :--- | :--- |
| **생산성** | Live Update / Prefab | **데이터 핫 리로드, DataManager** | ⭐⭐⭐⭐⭐ |
| **타격감** | AnimNotify / Shake | **애니메이션 이벤트, 카메라 쉐이크** | ⭐⭐⭐⭐⭐ |
| **전투** | Collision / Decal | **Jolt 기반 서버 판정, 장판 데칼** | ⭐⭐⭐⭐⭐ |
| **디버깅** | Visual Debugger | **Cheat Console, DebugDraw** | ⭐⭐⭐⭐ |
| **연출** | Post-Processing | **Bloom / Hit-Stop** | ⭐⭐⭐ |

---

## 4. 향후 로드맵 제언

1.  **1월 초:** `DataManager`와 `Hot-Reload`를 먼저 구축하여 **"수정-테스트" 반복 속도**를 올립니다.
2.  **1월 중:** `Jolt Physics`를 서버에 붙여 **정교한 이동과 판정**의 기초를 만듭니다.
3.  **1월 말:** 클라이언트에서 `Camera Shake`와 `Animation Event`를 연동하여 **최종적인 타격감**을 끌어올립니다.
